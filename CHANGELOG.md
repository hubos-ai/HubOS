# Changelog

## V1.4.0 — 记忆 / 上下文 / 子 Agent 体系重构

> 本版本同时包含 V1.3.0 飞书多用户调度优化的全部改动。上一正式发布为 V1.2.0，V1.3.0 飞书改动经一段时间观察后合并入本次发布。

### ✨ 核心改进（13 项）

#### 1. 长期记忆改为「工作区 + 用户」隔离
- 新增长期记忆账本 `workspace_ledger.py`，存储路径调整为 `<workspace>/.memory/users/<user-hash>/`
- 不同工作区、不同用户的事实记忆严格隔离，避免串用户、串项目
- 原始消息采用追加式归档，不直接覆盖历史记录
- 根据消息 ID 幂等写入，避免同一条消息被重复保存
- 工具调用记录与普通对话分开归档
- 压缩摘要也会写入长期记忆，后续可重新召回
- 临时子 Agent 不再重复写入父任务的长期记忆

#### 2. 上下文压缩不再直接删除历史
- Runner 不再通过时间清理直接丢弃旧消息
- 旧消息会先写入长期记忆账本，再从活跃上下文移除
- 过长历史按约 30,000 字符分块压缩
- 使用滚动摘要保留任务目标、决策、约束、未完成事项和关键成果
- ReMe 归档成功后才会从当前模型上下文中删除原始消息
- 修复带毫秒时间戳的解析问题，避免历史消息时间判断错误

#### 3. 大型工具调用参数和结果自动归档
- 已完成工具调用中的大参数不再长期跟随每次模型请求
- 大型工具输入和输出会保存到工作区归档文件
- 活跃上下文只保留：归档引用、简短预览、工具执行状态、必要的结果摘要
- 最近调用和尚未完成的调用仍保留完整数据，避免影响任务执行
- 解决工具读取大文件、网页或日志后，数据在后续每一轮被重复提交的问题

#### 4. 模型请求上下文预算保护
- 模型请求继续执行硬输入上限检查
- 为系统提示、工具定义和模型输出预留独立空间
- 压缩后的上下文必须处于模型可接受的输入预算内
- 避免历史消息、工具 Schema 和工具结果共同挤占输出空间
- 对 OpenAI 兼容模型及 GLM 等模型的上下文预算行为增加测试覆盖

#### 5. 子 Agent 改为明确的任务交接
- 子 Agent 现在会收到结构化交接信息：当前任务目标、必须遵守的约束、已知成果和文件、父任务压缩摘要、父会话 ID、父工作区路径
- 子 Agent 不再依赖复制父会话全部历史来理解任务

#### 6. 修复子 Agent 父上下文问题
- 修复 `parent_context` 的错误导入路径
- 工作流步骤会明确携带 `parent_session_id` 和交接上下文
- 子 Agent 的记忆召回指向父工作区，而不是临时目录或全局目录
- 防止子 Agent 找不到项目资料，或误读其他项目记忆

#### 7. 工作经验卡片继续全局共享
- 工作经验卡片仍保存在全局目录 `~/.hubos/work_experience_v4`
- 所有用户都可以复用通用的方法、排障经验和工作流程
- 工作区事实、用户对话和私人资料不会因为经验共享而跨用户传播
- 知识候选内容仍保持工作区本地化
- 最终边界：工作方法和通用经验可以共享，用户事实和项目隐私必须隔离

#### 8. 工作经验增加隐私脱敏
- 进入全局经验卡片前会清理：邮箱地址、手机号、飞书用户 ID、Token/密钥/凭据、用户目录和本地绝对路径、其他明显的用户身份信息
- 旧经验卡片在召回时也会先脱敏，并在后续保存过程中逐步清理

#### 9. 工作经验匹配状态按会话隔离
- 每个会话拥有独立的经验匹配状态和缓冲区
- 增加锁和有界缓存，避免并发会话互相覆盖
- 每一轮会固定本轮命中的经验卡片
- 修复用户连续快速发送消息时，下一轮匹配结果覆盖上一轮的问题

#### 10. 减少经验分类器输入量
- 不再把全部经验卡片都交给分类模型
- 先进行本地初筛，最多提交 24 张候选卡片
- 降低分类请求的 Token、延迟和超时风险
- 经验卡片存储增加锁和原子写入，降低并发写坏文件的风险

#### 11. 实际上下文优化结果
使用一个包含 164 条消息的真实飞书会话测试：

| 指标 | 优化前 | 优化后 | 变化 |
|------|--------|--------|------|
| 模型工具输入字符数 | 115,594 | 12,095 | **-89.5%** |
| 共归档大型工具调用 | — | 9 个 | — |
| 单个最大工具输入 | 21,373 | 2,538 | -88.1% |

#### 12. 验证与上线状态
- 单元及集成测试：**121 passed, 9 skipped**
- Ruff 代码检查通过
- Python 编译检查通过
- `git diff --check` 通过
- HubOS 服务已重启，飞书 WebSocket 和 FeishuGateway 已正常启动
- MCP 服务连接正常，重启时没有正在执行的任务被中断

#### 13. 使用行为变化
- 特别大的旧会话第一次继续使用时，会进行一次历史归档和分块压缩，因此首次响应可能比普通消息慢几秒
- 完成迁移后只会增量处理新消息，不会再反复向模型提交全部历史

### 📦 包含的 V1.3.0 飞书多用户调度优化
- 飞书多用户路由：每个飞书 open_id 对应独立 workspace（`feishu_<open_id>`）
- FeishuGateway 单 WebSocket 连接共享，请求精准隔离
- LLM fallback：GLM-5.1 → DeepSeek V4 Flash
- 飞书长任务优先 `delegate_task(wait=False)`，避免用户长时间无反馈
- research 快速调研契约：2-3 min，10 次搜索 + 6 次阅读，不写长报告
- 启动预热优化：warmup 从 ~22s 降到 ~3s
- 600 秒绝对超时修复：基于空闲时间而非总时长
- 飞书发图片链路修复：`DeliveryContext` 直接发送
- 新飞书用户使用 PROFILE/SOUL/AGENTS 引导模板，不复制 default

---

## V1.2.0 — 知识管理与架构升级

### ✨ 新增功能

- **Knowledge Injection 统一注入层**：自动语义检索 `memory/knowledge/*.md`，五维评分（标题/实体/摘要/标签/置信度），按任务深度自动分配 token 预算（Light=0/Normal=300/Deep=600/参考历史=1000）
- **RunPolicy + AutoRouter 任务决策引擎**：LIGHT/NORMAL/DEEP 三级前置分类，简单问答跳过管线节省 2 次 LLM 调用，纯正则驱动零额外开销
- **工具输出归档（refs 系统）**：长工具输出外置到 `refs/{session_id}/`，三级自动清理（4h 活跃保护 + 7天过期 + 500MB 容量淘汰）
- **知识库每日维护**：`knowledge_pending/` 自动分类、敏感信息过滤、合并到正式知识库文件
- **经验卡片 Schema v4.1**：新增 `experience_type`（11 种类型白名单）+ `entities`（实体提取去重）字段
- **Cron 模型覆盖机制**：ContextVar 传递 model_override，cron 任务独立指定模型，不影响 default agent 日常对话
- **前端 StatusToolCard 组件**：状态卡前端渲染

### ⚡ 优化

- **Runner 架构重构**：Agent 池化复用（避免重复构建工具集）、三阶段前置管线标准化
- **ContextVar 上下文系统**：统一 session_id / agent_id / model_override 传递，子 Agent 写文件范围约束
- **Work Experience 追溯增强**：经验卡带 ref_session_id + ref_agent_id，支持前端跳回来源会话
- **reasoning_content 支持**：适配 DeepSeek thinking 块、GLM extended thinking

### 🐛 修复

- **内部状态卡协议修复**：从 `tool_use` 伪协议改为独立 `hubos_status` 协议，三层防火墙隔离，消除模型幻觉调用伪工具的根因
- **Cron 执行器 input 格式兼容**：`_normalize_runner_input()` 解决 legacy string 到 dict 格式兼容，旧 cron 任务恢复运行
- **cleanup_refs() 账目 bug**：删除成功才扣账，失败时 session 保留
- **ContextVar finally 清理**：跨任务 session_id 泄漏风险修复
- **GLM MCP 配置适配**：SSE → Streamable HTTP 协议迁移
